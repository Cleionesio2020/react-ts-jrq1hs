import * as React from 'react';
import './style.css';

const Main: React.FC = ({ children }) => {
  return (
    <div>

  <div className="container">
  
    
    {children}</div>
  
  </div>
  );
};

export default Main;
